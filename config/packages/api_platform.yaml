api_platform:
    title: PIM API Platform
    version: 1.0.0
    defaults:
        stateless: true
        cache_headers:
            vary: ['Content-Type', 'Authorization', 'Origin']

    formats:
        json: ['application/json']

    error_formats:
        jsonproblem: ['application/problem+json']
        json: ['application/json']
